To 'verify' the tables in your database you can use the 'select'(grab data) statement combined 
with the '*' (All columns) symbol followed by the table name you want to 'grab'. In this case,
we repeated the statement for all tables.